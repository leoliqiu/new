{
  "name": "vscode-ai-bridge",
  "displayName": "Playwright VS Code Agent Bridge",
  "description": "A local-only bridge from Playwright Command Studio to VS Code language models and bounded workspace-reading agent tools.",
  "version": "0.3.0",
  "publisher": "local",
  "private": true,
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:vscodeAiBridge.start",
    "onCommand:vscodeAiBridge.stop"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscodeAiBridge.start",
        "title": "Playwright VS Code Agent Bridge: Start"
      },
      {
        "command": "vscodeAiBridge.stop",
        "title": "Playwright VS Code Agent Bridge: Stop"
      }
    ],
    "configuration": {
      "title": "Playwright VS Code Agent Bridge",
      "properties": {
        "vscodeAiBridge.port": {
          "type": "number",
          "default": 32123,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Local TCP port used by Playwright Command Studio."
        },
        "vscodeAiBridge.maxAgentTurns": {
          "type": "number",
          "default": 8,
          "minimum": 1,
          "maximum": 12,
          "description": "Maximum read-only tool turns for one external agent request."
        }
      }
    }
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
